# Contributing to Chatman.media

Спасибо за интерес к проекту! Этот документ описывает процесс разработки.

## 🚀 Быстрый старт

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/yourusername/chatman-media-community.git
cd chatman-media-community
```

2. **Установите зависимости**
```bash
bun install
```

3. **Запустите базы данных**
```bash
bun run docker:up
```

4. **Настройте переменные окружения**
```bash
cp apps/web/.env.local.example apps/web/.env.local
cp apps/api/.env.example apps/api/.env
```

5. **Запустите dev сервер**
```bash
bun run dev
```

## 📝 Git Workflow

### Создание изменений

1. Создайте новую ветку:
```bash
git checkout -b feature/amazing-feature
```

2. Сделайте изменения

3. Закоммитьте изменения:
```bash
git add .
git commit -m "Описание изменений"
```

**Автоматические проверки при коммите:**
- ✅ Biome проверит и исправит форматирование
- ✅ TypeScript проверит типы
- ✅ Валидация сообщения коммита (минимум 10 символов)

4. Запустите тесты вручную:
```bash
bun run test
```

5. Создайте Pull Request

### Формат коммитов

Мы используем неформальный формат коммитов на русском языке:

```
Краткое описание изменений

Подробное описание если нужно:
- Что изменено
- Почему изменено
- Какие проблемы решены
```

Примеры хороших коммитов:
```
Добавить компонент загрузки файлов

Исправить ошибку валидации email

Оптимизировать запросы к базе данных
```

### Pre-commit хуки

При каждом коммите автоматически запускаются:

1. **lint-staged** - проверка только измененных файлов:
   - Biome lint + format
   - Автоисправление где возможно

2. **typecheck** - проверка типов TypeScript:
   - Быстрая проверка благодаря Turbo cache
   - Проверяются все пакеты в монорепо

3. **commit-msg** - валидация сообщения:
   - Минимум 10 символов
   - Не пустое

### Обход хуков (не рекомендуется)

Если срочно нужно закоммитить без проверок:
```bash
git commit --no-verify -m "urgent fix"
```

⚠️ **Внимание**: CI все равно проверит код при push.

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
bun run test

# Конкретный пакет
cd apps/api && bun test
cd apps/web && bunx vitest run

# Все проверки перед push
bun run validate
```

### Написание тестов

- API tests: `*.test.ts` в `apps/api/src/`
- Web tests: `*.test.tsx` в `apps/web/`
- Package tests: `*.test.ts` в `packages/*/src/`

Пример теста:
```typescript
import { describe, expect, it } from 'vitest'

describe('Feature', () => {
  it('should work correctly', () => {
    expect(1 + 1).toBe(2)
  })
})
```

## 🔧 Доступные команды

```bash
# Разработка
bun run dev          # Запуск dev серверов
bun run build        # Сборка для продакшена
bun run clean        # Очистка node_modules и build артефактов

# Качество кода
bun run lint         # Проверка с Biome
bun run format       # Форматирование кода
bun run check        # Lint + format + auto-fix
bun run typecheck    # Проверка TypeScript типов
bun run test         # Запуск всех тестов
bun run validate     # Все проверки сразу

# Docker
bun run docker:up    # Запуск PostgreSQL + Redis
bun run docker:down  # Остановка Docker контейнеров
bun run docker:logs  # Логи Docker контейнеров
```

## 📦 Структура проекта

```
chatman-media-community/
├── apps/
│   ├── web/              # Next.js frontend (port 2000)
│   └── api/              # Express backend (port 2001)
├── packages/
│   ├── shared/           # Общие типы и утилиты
│   ├── ui/              # UI компоненты (пока не используется)
│   └── config/          # Конфигурации
├── .github/             # GitHub Actions workflows
├── .husky/              # Git hooks
└── docker-compose.yml   # PostgreSQL + Redis
```

## 🎨 Стиль кода

Мы используем **Biome** для линтинга и форматирования:

- Одинарные кавычки
- Точка с запятой опциональна
- 2 пробела для отступов
- Максимальная длина строки: 100 символов

Biome автоматически исправляет код при коммите.

## 🐛 Отладка

### Проблемы с хуками

Если хуки не работают:
```bash
bun run prepare
```

### Проблемы с базой данных

```bash
# Перезапуск Docker контейнеров
bun run docker:down
bun run docker:up

# Проверка логов
bun run docker:logs
```

### Проблемы с типами

```bash
# Очистить кэш TypeScript
rm -rf apps/*/tsconfig.tsbuildinfo

# Переустановить зависимости
bun run clean
bun install
```

## 📄 Pull Request процесс

1. Убедитесь что все тесты проходят:
```bash
bun run validate
```

2. Обновите документацию если нужно

3. Создайте PR с описанием:
   - Что изменено
   - Почему изменено
   - Как протестировать

4. Дождитесь прохождения CI checks

5. Ревью от maintainers

## 🤝 Помощь

- GitHub Issues - для багов и feature requests
- GitHub Discussions - для вопросов и обсуждений
- Telegram группа - для общения

## 📚 Дополнительная документация

- [SETUP.md](./SETUP.md) - Подробная инструкция по настройке
- [DEPLOYMENT.md](./DEPLOYMENT.md) - Деплой в продакшен
- [CLAUDE.md](./CLAUDE.md) - Архитектура для AI ассистентов
- [.github/README.md](./.github/README.md) - CI/CD workflows
- [.husky/README.md](./.husky/README.md) - Git hooks

---

**Спасибо за ваш вклад!** ❤️
