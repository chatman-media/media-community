{
  "name": "@chatman/shared",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint src/**/*.ts",
    "test": "vitest --run --passWithNoTests",
    "clean": "rm -rf .turbo node_modules"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.7"
  }
}
